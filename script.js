//classList.toggle('hidden') adiciona a classe hidden se ela não estiver presente, ou remove se já estiver.
function alteraAparenciaMenu(id) {
        document.getElementById(id).classList.toggle('hidden');
}